{#
/**
 * @file applicant_users.html.twig
 * Default theme implementation to present Applicant users data.
 *
 * This template is used when viewing Applicant users pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_applicant_users()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('applicant_users') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
