{#
/**
 * @file gel_region.html.twig
 * Default theme implementation to present Gel region data.
 *
 * This template is used when viewing Gel region pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_gel_region()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('gel_region') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
